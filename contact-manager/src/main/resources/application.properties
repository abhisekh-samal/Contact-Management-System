spring.application.name=contact-manager

# MySQL configuration
# Option A (recommended): set env vars DB_URL, DB_USER, DB_PASSWORD
# Option B: replace the values directly here.
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/contact_manager?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=root
spring.datasource.password=abhi1a2b3c


spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=never
